{
  "name": "energy-attestation-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:amoy": "graph deploy --node https://api.studio.thegraph.com/deploy/ --deploy-key $GRAPH_DEPLOY_KEY energy-attestation-service-amoy",
    "deploy:polygon": "graph deploy --node https://api.studio.thegraph.com/deploy/ --deploy-key $GRAPH_DEPLOY_KEY energy-attestation-service"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1"
  }
}
